<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.presentator">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".modules.friends.UserItemActivity" />
        <activity android:name=".modules.newsFeed.NewsItem" />
        <activity android:name=".modules.addNew.PresentAddingActivity" />
        <activity android:name=".modules.auth.signIn.SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".modules.auth.signUp.SignUpActivity" />
        <activity android:name=".modules.profile.ProfileActivity" />
        <activity android:name=".modules.friends.FriendsActivity" />
        <activity android:name=".modules.newsFeed.NewsFeedActivity" />
        <activity android:name=".modules.giftsList.GiftsListActivity" />
    </application>

</manifest>